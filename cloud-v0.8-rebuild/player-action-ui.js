/* ClubMatch Cloud v0.8 - fast coach actions, team possession and optional analyst ball-flow */
(function(global){
'use strict';
const MODE_KEY='clubmatch.v08.action.mode';
const ACTIONS=Object.freeze([
 {id:'possession_control',label:'Balcontrole',group:'Bal'},{id:'ball_recovery',label:'Bal veroverd',group:'Bal'},{id:'interception',label:'Onderschepping',group:'Bal'},{id:'block',label:'Blok',group:'Bal'},{id:'ball_loss',label:'Balverlies',group:'Bal'},
 {id:'bad_pass',label:'Verkeerde pass',group:'Pass'},{id:'chance_created',label:'Kans gecreëerd',group:'Pass'},
 {id:'duel_won',label:'Duel gewonnen',group:'Duel'},{id:'duel_lost',label:'Duel verloren',group:'Duel'},
 {id:'shot',label:'Schot',group:'Aanval'},{id:'shot_on_target',label:'Schot op doel',group:'Aanval'},
 {id:'foul_committed',label:'Overtreding / vrije trap tegen',group:'Spelhervatting'},{id:'foul_won',label:'Overtreding mee / vrije trap mee',group:'Spelhervatting'},
 {id:'save',label:'Redding',group:'Overig'},{id:'injury',label:'Blessure',group:'Overig'}
]);
const LOSS_LABELS=Object.freeze([['bad_pass','Foute pass'],['duel_lost','Duel'],['poor_control','Controle'],['dribble','Dribbel'],['interception','Onderschept'],['other','Overig']]);
const GOAL_TYPES=Object.freeze([['open_play','Open spel'],['counter','Counter'],['set_piece','Standaardsituatie'],['corner','Corner'],['free_kick','Vrije trap'],['penalty','Penalty'],['own_goal','Eigen doelpunt'],['other','Overig']]);
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function getMode(){try{return global.localStorage?.getItem(MODE_KEY)||'coach'}catch{return 'coach'}}function setMode(v){try{global.localStorage?.setItem(MODE_KEY,v)}catch{}}
function createPlayerActionUi(options={}){
 const doc=options.document||global.document,controller=options.controller,runtime=options.runtime,run=options.run||((_,fn)=>fn());if(!doc||!controller)throw new Error('Document en speleractie-controller zijn verplicht');let sheet=null,currentPlayerId=null,posBar=null,timer=null,mode=getMode();
 function ensureStyles(){if(doc.getElementById('v08PlayerActionStyles'))return;const s=doc.createElement('style');s.id='v08PlayerActionStyles';s.textContent=`.paOverlay{position:fixed;inset:0;background:rgba(30,18,43,.35);z-index:9997;display:flex;align-items:flex-end;justify-content:center}.paSheet{width:min(720px,100%);max-height:82vh;overflow:auto;background:#fff;border-radius:18px 18px 0 0;padding:14px;box-shadow:0 -8px 28px rgba(40,20,60,.22)}.paHead{display:flex;align-items:flex-start;justify-content:space-between;gap:10px}.paClose{border:0;background:#f1e9f8;color:#4b2672;border-radius:999px;padding:7px 10px;font-weight:900;cursor:pointer}.paGroup{margin-top:10px}.paGroup b{display:block;color:#4b2672;font-size:11px;margin-bottom:5px}.paButtons{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:6px}.paAction{border:1px solid #7e5ca4;background:#faf7fd;color:#4b2672;border-radius:10px;padding:9px 7px;font-weight:800;cursor:pointer;text-align:center}.paPossession{border:2px solid #1976d2;background:#eef7ff;color:#125a9e}.paHint{margin-top:7px;padding:7px 9px;border-radius:9px;background:#f7f4fa;color:#6b5877;font-size:10px}.posBar{margin-top:9px;padding:10px 11px;border:1px solid #b8d8f5;border-radius:11px;background:#f4faff}.posHead{display:flex;gap:8px;align-items:center;justify-content:space-between;flex-wrap:wrap}.posClock{font-variant-numeric:tabular-nums;font-weight:900;color:#125a9e}.teamFlow{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px;margin:8px 0}.teamFlow button{padding:10px 8px!important}.teamFor.active{background:#237a43!important;color:#fff!important}.teamAgainst.active{background:#983263!important;color:#fff!important}.flowGrid{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:5px;margin-top:7px}.flowPlayer{background:#fff!important;color:#4b2672!important;border:1px solid #8d6dac!important;padding:6px 4px!important;font-size:10px!important}.flowPlayer.active{background:#1976d2!important;color:#fff!important;border-color:#1976d2!important}.lossGrid{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:5px;margin-top:7px}.lossBtn{background:#fff!important;color:#983263!important;border:1px solid #b95078!important;padding:6px 4px!important;font-size:9px!important}.modeBtn{background:#fff!important;color:#125a9e!important;border:1px solid #1976d2!important}.modeBtn.on{background:#1976d2!important;color:#fff!important}.paGoal{border:2px solid #9ed5ad;background:#eef9f1;border-radius:11px;padding:9px}.paGoalGrid{display:grid;grid-template-columns:1fr 1fr auto;gap:6px;align-items:end}.paGoalBtn{background:#237a43!important;color:#fff!important;border:0!important;padding:10px!important}@media(max-width:650px){.flowGrid,.lossGrid{grid-template-columns:repeat(3,minmax(0,1fr))}.paButtons{grid-template-columns:repeat(2,minmax(0,1fr))}.paSheet{max-height:88vh}.paGoalGrid{grid-template-columns:1fr}.teamFlow{grid-template-columns:1fr 1fr}.teamFlow button:last-child{grid-column:1/-1}}`;doc.head.appendChild(s)}
 function player(){return (controller.snapshot?.players||[]).find(p=>p.player_id===currentPlayerId)||null}
 function playerName(p){return p?`#${p.shirt_number??'—'} ${p.display_name||p.full_name||p.player_id}`:'Speler'}
 function fieldPlayers(){return (controller.snapshot?.players||[]).filter(p=>p.selected&&p.is_on_field)}
 function currentClockSecond(){const raw=doc.getElementById('v08Clock')?.textContent||'0:00',parts=raw.split(':').map(Number);return Math.max(0,(parts[0]||0)*60+(parts[1]||0))}
 function ensurePossessionBar(){if(posBar?.isConnected)return posBar;ensureStyles();const pitch=doc.getElementById('v08Pitch'),card=pitch?.closest('.card');if(!card)return null;posBar=doc.createElement('div');posBar.id='v08PossessionBar';posBar.className='posBar';card.appendChild(posBar);renderPossessionBar();if(!timer)timer=setInterval(renderPossessionBar,1000);return posBar}
 function analystHtml(active){if(mode!=='analyst')return '';const players=fieldPlayers();return `<div class="paHint"><b>Analistmodus:</b> tik vooral de volgende speler die de bal krijgt. ClubMatch registreert automatisch geslaagde pass + ontvangen pass + spelerbezit. Team-balbezit hierboven blijft een aparte, snellere teller.</div><div class="flowGrid">${players.map(p=>`<button type="button" class="flowPlayer ${active?.playerId===p.player_id?'active':''}" data-flow-player="${esc(p.player_id)}" data-v08-action>#${esc(p.shirt_number??'—')} ${esc(p.display_name||p.full_name||'')}</button>`).join('')}</div>${active?`<div class="lossGrid">${LOSS_LABELS.map(([id,label])=>`<button type="button" class="lossBtn" data-loss-cause="${id}" data-v08-action>${esc(label)}</button>`).join('')}</div>`:''}`}
 function renderPossessionBar(){if(!posBar?.isConnected)return;const active=controller.currentPossession?.(),team=controller.currentTeamPossession?.(),now=currentClockSecond(),playerElapsed=active?Math.max(0,now-Number(active.startedSecond||0)):0,teamElapsed=team?Math.max(0,now-Number(team.startedSecond||0)):0;posBar.innerHTML=`<div class="posHead"><div><b>⚽ Snelle registratie</b><div class="muted">Team-balbezit kan zonder speler worden bijgehouden. Spelerbezit is optioneel voor diepere analyse.</div></div><button type="button" id="v08AnalystMode" class="modeBtn ${mode==='analyst'?'on':''}" data-v08-action>${mode==='analyst'?'Analistmodus aan':'Analistmodus'}</button></div><div class="teamFlow"><button type="button" class="teamFor ${team?.side==='for'?'active':''}" data-team-possession="for" data-v08-action>🟢 Ons bezit${team?.side==='for'?` · ${Math.floor(teamElapsed/60)}:${String(teamElapsed%60).padStart(2,'0')}`:''}</button><button type="button" class="teamAgainst ${team?.side==='against'?'active':''}" data-team-possession="against" data-v08-action>🔴 Tegenstander${team?.side==='against'?` · ${Math.floor(teamElapsed/60)}:${String(teamElapsed%60).padStart(2,'0')}`:''}</button><button type="button" class="secondary" id="v08StopTeamPossession" data-v08-action>■ Bezit stoppen</button></div><div class="posHead"><div><b>Spelerbezit</b><div class="muted">${active?esc(`#${active.shirtNumber??'—'} ${active.name}`):'Geen actieve spelerbezitter'}</div></div>${active?`<span class="posClock">${Math.floor(playerElapsed/60)}:${String(playerElapsed%60).padStart(2,'0')}</span>`:''}</div>${analystHtml(active)}`;posBar.querySelector('#v08AnalystMode').onclick=()=>{mode=mode==='analyst'?'coach':'analyst';setMode(mode);renderPossessionBar()};posBar.querySelectorAll('[data-team-possession]').forEach(btn=>btn.onclick=()=>run('Team-balbezit',async()=>{await controller.startTeamPossession(btn.dataset.teamPossession);renderPossessionBar()}));posBar.querySelector('#v08StopTeamPossession').onclick=()=>run('Team-balbezit stoppen',async()=>{await controller.stopTeamPossession();renderPossessionBar()});posBar.querySelectorAll('[data-flow-player]').forEach(btn=>btn.onclick=()=>run('Balstroom vastleggen',async()=>{await controller.receiveBall(btn.dataset.flowPlayer);renderPossessionBar()}));posBar.querySelectorAll('[data-loss-cause]').forEach(btn=>btn.onclick=()=>run('Balverlies vastleggen',async()=>{const a=controller.currentPossession?.();if(!a)throw new Error('Geen actieve balbezitter');await controller.loseBall(a.playerId,btn.dataset.lossCause);renderPossessionBar()}))}
 function close(){currentPlayerId=null;if(sheet){sheet.remove();sheet=null}}
 function assistOptions(scorerId){return '<option value="">— geen / onbekend —</option>'+fieldPlayers().filter(p=>p.player_id!==scorerId).map(p=>`<option value="${esc(p.player_id)}">#${esc(p.shirt_number??'—')} ${esc(p.display_name||p.full_name||'')}</option>`).join('')}
 function goalTypeOptions(){return GOAL_TYPES.map(([id,label])=>`<option value="${id}">${label}</option>`).join('')}
 function open(playerId){currentPlayerId=playerId;const p=player();if(!p)return;close();currentPlayerId=playerId;ensureStyles();sheet=doc.createElement('div');sheet.className='paOverlay';const groups=[...new Set(ACTIONS.map(a=>a.group))],active=controller.currentPossession?.();sheet.innerHTML=`<div class="paSheet" role="dialog" aria-modal="true"><div class="paHead"><div><h2 style="margin:0">⚡ Snelle speleractie</h2><div class="muted">${esc(playerName(p))} · ${p.is_on_field?'VELD':'BANK'}</div></div><button type="button" class="paClose">Sluiten</button></div>${p.is_on_field&&runtime?.recordGoal?`<div class="paGroup paGoal"><b>⚽ Doelpunt gemaakt</b><div class="paGoalGrid"><label>Type<select id="paGoalType">${goalTypeOptions()}</select></label><label>Assist<select id="paGoalAssist">${assistOptions(p.player_id)}</select></label><button type="button" id="paGoalBtn" class="paGoalBtn" data-v08-action>Doelpunt opslaan</button></div></div>`:''}${p.is_on_field?`<div class="paGroup"><b>Spelerbezit</b><div class="paButtons"><button type="button" class="paAction paPossession" data-possession-start>${active?.playerId===p.player_id?'✓ Bezit loopt':'▶ Bezit starten'}</button>${active?'<button type="button" class="paAction" data-possession-stop>Spelerbezit stoppen</button>':''}</div></div>`:''}${groups.map(group=>`<div class="paGroup"><b>${esc(group)}</b><div class="paButtons">${ACTIONS.filter(a=>a.group===group).map(a=>`<button type="button" class="paAction" data-pa-action="${a.id}"${!p.is_on_field&&a.id!=='injury'?' disabled':''}>${esc(a.label)}</button>`).join('')}</div></div>`).join('')}<label style="display:block;margin-top:10px">Notitie (optioneel)<input id="paNote" placeholder="Bijv. context of blessure"></label><div class="paHint"><b>Coachmodus</b> = alleen wat je betrouwbaar kunt bijhouden. Voor snelle teamverhoudingen gebruik je de twee grote balbezitknoppen onder het veld.</div></div>`;doc.body.appendChild(sheet);sheet.querySelector('.paClose').onclick=close;sheet.addEventListener('click',e=>{if(e.target===sheet)close()});sheet.querySelector('#paGoalBtn')?.addEventListener('click',()=>run('Doelpunt opslaan',async()=>{await runtime.recordGoal({side:'for',scorerId:currentPlayerId,assistId:sheet?.querySelector('#paGoalAssist')?.value||null,goalType:sheet?.querySelector('#paGoalType')?.value||null,note:sheet?.querySelector('#paNote')?.value||null});close();renderPossessionBar()}));sheet.querySelector('[data-possession-start]')?.addEventListener('click',()=>run('Spelerbezit starten',async()=>{await controller.startPossession(currentPlayerId);close();renderPossessionBar()}));sheet.querySelector('[data-possession-stop]')?.addEventListener('click',()=>run('Spelerbezit stoppen',async()=>{await controller.stopPossession();close();renderPossessionBar()}));sheet.querySelectorAll('[data-pa-action]').forEach(btn=>btn.addEventListener('click',()=>run('Speleractie opslaan',async()=>{const action=btn.dataset.paAction,note=sheet?.querySelector('#paNote')?.value||'';if(action==='ball_loss'||action==='bad_pass'){const cause=action==='bad_pass'?'bad_pass':'unknown';await controller.loseBall(currentPlayerId,cause,note)}else await controller.record(currentPlayerId,action,note);close();renderPossessionBar()})));return sheet}
 function install(){ensureStyles();doc.addEventListener('click',event=>{const button=event.target.closest?.('[data-player-action-open]');if(!button)return;event.preventDefault();event.stopPropagation();const id=button.dataset.playerId||button.closest?.('[data-player-id]')?.dataset.playerId;if(id)open(id)});setTimeout(ensurePossessionBar,0);return true}
 function refresh(){ensurePossessionBar();renderPossessionBar()}
 return Object.freeze({install,open,close,refresh,get visible(){return !!sheet},get mode(){return mode}})
}
global.ClubMatchV08PlayerActionUi={ACTIONS,LOSS_LABELS,GOAL_TYPES,createPlayerActionUi};
})(typeof window!=='undefined'?window:globalThis);
