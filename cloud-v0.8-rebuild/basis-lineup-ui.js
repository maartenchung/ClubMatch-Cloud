/* ClubMatch Cloud v0.8 - historische basisopstelling zichtbaar bij lopende wedstrijden */
(function(global){
'use strict';
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function createBasisLineupUi(options={}){
  const doc=options.document||global.document;if(!doc)throw new Error('Document is verplicht');let panel=null,dragId='';
  function ensureStyles(){if(doc.getElementById('v08BasisStyles'))return;const s=doc.createElement('style');s.id='v08BasisStyles';s.textContent=`.basisPitch{position:relative;min-height:520px;border-radius:16px;background:linear-gradient(90deg,#3f8b58,#4a9962);border:4px solid #fff;overflow:hidden;box-shadow:inset 0 0 0 1px rgba(255,255,255,.5)}.basisLine{position:absolute;pointer-events:none;border:2px solid rgba(255,255,255,.8);z-index:0}.basisHalf{left:0;right:0;top:50%;border-width:2px 0 0}.basisCircle{width:105px;height:105px;border-radius:50%;left:50%;top:50%;transform:translate(-50%,-50%)}.basisBoxTop,.basisBoxBottom{width:62%;height:18%;left:19%}.basisBoxTop{top:-2px}.basisBoxBottom{bottom:-2px}.basisSlot{position:absolute;width:122px;min-height:75px;transform:translate(-50%,-50%);z-index:2;border:2px dashed rgba(255,255,255,.75);border-radius:12px;background:rgba(255,255,255,.12)}.basisSlot.dragOver{outline:4px solid #f3c44f}.basisPlayer{width:100%;min-height:72px;background:rgba(255,255,255,.96);border-radius:10px;padding:6px;text-align:center;box-shadow:0 2px 8px rgba(0,0,0,.14)}.basisPlayer[draggable=true]{cursor:grab}.basisPos{font-size:9px;font-weight:900;color:#4b2672}.basisName{font-size:11px;font-weight:900;margin-top:3px}.basisFull{font-size:8px;color:#6b5877}.basisReadonly{font-size:10px;color:#6b5877;margin:4px 0 8px}@media(max-width:560px){.basisPitch{min-height:470px}.basisSlot{width:100px;min-height:66px}.basisPlayer{min-height:63px;padding:4px}.basisName{font-size:9px}.basisFull{display:none}}`;doc.head.appendChild(s)}
  function ensureMounted(){if(panel)return panel;ensureStyles();const app=doc.getElementById('appPanel');if(!app)throw new Error('Appscherm ontbreekt');panel=doc.createElement('section');panel.id='v08BasisLineup';panel.className='card hidden';const active=doc.getElementById('activeMatchTitle')?.closest('.card');if(active)active.after(panel);else app.prepend(panel);return panel}
  function lines(){return '<i class="basisLine basisHalf"></i><i class="basisLine basisCircle"></i><i class="basisLine basisBoxTop"></i><i class="basisLine basisBoxBottom"></i>'}
  function hasSub(snapshot){return (snapshot?.events||[]).some(e=>e.event_type==='substitution')}
  function render(snapshot){const root=ensureMounted();if(!snapshot?.match?.id){root.classList.add('hidden');root.replaceChildren();return root}const formation=snapshot.match.formation_code||'4-3-3',geom=global.ClubMatchV08PitchLayout?.geometry?.(formation);if(!geom)return root;const starters=(snapshot.players||[]).filter(p=>p.is_starter),byPos=new Map(starters.map(p=>[p.starting_position,p])),editable=snapshot.match.status==='live'&&!hasSub(snapshot)&&typeof options.onMove==='function';root.innerHTML=`<div class="sectionTitle"><div><h2 style="margin-bottom:3px">Basisopstelling · ${esc(formation)}</h2><div class="muted">Historische startopstelling blijft zichtbaar bij bestaande en lopende wedstrijden.</div></div><span class="badge">${editable?'BEWERKBAAR TOT 1E WISSEL':'HISTORISCH'}</span></div><div class="basisReadonly">${editable?'Sleep binnen de basis om posities te verschuiven. De Cloud-basis én live opstelling worden samen bijgewerkt.':'Na de eerste wissel wordt de basis niet meer gewijzigd; live veranderingen staan in de live opstelling en live monitoring.'}</div><div class="basisPitch">${lines()}${geom.slots.map(slot=>{const p=byPos.get(slot.position);return `<div class="basisSlot" data-position="${esc(slot.position)}" style="left:${slot.x}%;top:${slot.y}%">${p?`<div class="basisPlayer" data-player-id="${esc(p.player_id)}" draggable="${editable?'true':'false'}"><div class="basisPos">${esc(slot.position)}</div><div class="basisFull">${esc(slot.label)}</div><div class="basisName">#${esc(p.shirt_number??'—')} ${esc(p.display_name||p.full_name||p.player_id)}</div></div>`:''}</div>`}).join('')}</div>`;bind(root,editable);root.classList.remove('hidden');return root}
  function bind(root,editable){if(!editable)return;root.querySelectorAll('.basisPlayer').forEach(node=>node.addEventListener('dragstart',e=>{dragId=node.dataset.playerId;e.dataTransfer?.setData('text/plain',dragId)}));root.querySelectorAll('.basisSlot').forEach(slot=>{slot.addEventListener('dragover',e=>{e.preventDefault();slot.classList.add('dragOver')});slot.addEventListener('dragleave',()=>slot.classList.remove('dragOver'));slot.addEventListener('drop',e=>{e.preventDefault();slot.classList.remove('dragOver');const id=e.dataTransfer?.getData('text/plain')||dragId;if(id)options.onMove(id,slot.dataset.position)})})}
  function clear(){const root=ensureMounted();root.replaceChildren();root.classList.add('hidden')}
  return Object.freeze({render,clear,get visible(){return panel&&!panel.classList.contains('hidden')}})
}
global.ClubMatchV08BasisLineup={createBasisLineupUi};
})(typeof window!=='undefined'?window:globalThis);
