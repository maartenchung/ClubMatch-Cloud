import fs from 'node:fs';import assert from 'node:assert/strict';const read=f=>fs.readFileSync(new URL(`./${f}`,import.meta.url),'utf8');
const html=read('index.html'),feed=read('event-feed-ui.js'),notice=read('live-notice-ui.js'),standalone=read('action-field-standalone.js'),roadmap=read('roadmap-ux-v08.js'),playerUi=read('player-action-ui.js'),controller=read('player-action-controller.js'),actionField=read('action-field-ui.js'),view=read('view-model.js'),stabilize=read('v08-stabilization.js');
assert.match(html,/build 20260828\.2145/);assert.match(html,/live-notice-ui\.js\?v=20260828\.2145/);
assert.match(feed,/Wedstrijd/);assert.match(feed,/Analyse/);assert.match(feed,/Toon oudere gebeurtenissen/);assert.match(feed,/visibleLimit=12/);assert.match(feed,/DOMContentLoaded/,'eventfeed moet zichzelf starten');
assert.match(notice,/Laatste melding/);assert.match(notice,/automatic_deadline_stop/);assert.match(notice,/navigator\?\.vibrate/);assert.match(notice,/sessionStorage/);assert.match(notice,/\+ Blessuretijd/);
assert.doesNotMatch(standalone,/get_match_snapshot/);assert.match(standalone,/clubmatch:v08-confirmed/);assert.match(roadmap,/Actieveld = waar en hoe/);assert.match(roadmap,/afQuick/);
assert.match(playerUi,/Alles bezit stoppen/);assert.match(playerUi,/stopAllPossession/);assert.match(playerUi,/analystReceiveBall/);assert.match(playerUi,/if\(mode==='analyst'\).*stopPossession/s);
assert.match(playerUi,/Categorieën/);assert.match(playerUi,/A–Z/);assert.match(playerUi,/QUICK_ORDER_KEY/);assert.match(playerUi,/data-quick-action/);assert.match(playerUi,/data-quick-side/);assert.match(playerUi,/v08QuickPlayer/);assert.match(playerUi,/Goal/);assert.match(playerUi,/Schot op doel/);assert.match(playerUi,/Vrije trap/);assert.match(playerUi,/Corner/);
const afQuick=actionField.match(/const QUICK=(\[[^\]]+\])/s)?.[1]||'';const playerQuick=playerUi.match(/const FIELD_QUICK=Object\.freeze\((\[[^\]]+\])\)/s)?.[1]||'';assert.equal(playerQuick.replace(/\s/g,''),afQuick.replace(/\s/g,''),'Snelle registratie en Actieveld moeten dezelfde zichtbare actielijst hebben');
assert.match(controller,/recordQuickAction/);assert.match(controller,/record_action_field_event_v08/);assert.match(controller,/stop_all_possession_v08/);assert.match(controller,/record_analyst_ball_flow_v08/);assert.match(controller,/cause==='bad_pass'\?'bad_pass':'ball_loss'/);
assert.match(view,/enrichClockDurations/);assert.match(view,/pause_duration_seconds/);assert.match(stabilize,/clubmatch:v08-notice/);assert.match(stabilize,/captureSharedClient/);assert.doesNotMatch(stabilize,/client\.rpc\s*=/,'Supabase rpc mag nooit worden overschreven: property kan read-only zijn');
console.log('PASS live-ux-fixes: quick parity + category/A-Z preference + read-only rpc safe refresh + previous live gates');
