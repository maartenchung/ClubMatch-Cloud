/* ClubMatch Cloud v0.8 - visuele wedstrijdvoorbereiding met drag & drop + favorites */
(function(global){
'use strict';
function esc(value){return String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]))}
function createPreparationUi(options={}){
  const doc=options.document||global.document,controller=options.controller;if(!doc||!controller)throw new Error('Document en wedstrijdvoorbereiding zijn verplicht');
  let panel=null,lastState=null,dragPlayerId='',dragFavoriteId='',favoriteDraft='';
  function layout(){return global.ClubMatchV08PitchLayout}
  function ensureStyles(){if(doc.getElementById('v08PrepStyles'))return;const s=doc.createElement('style');s.id='v08PrepStyles';s.textContent=`
    .prepWorkspace{display:grid;grid-template-columns:minmax(360px,1.08fr) minmax(330px,.92fr);gap:14px;margin-top:10px}.prepPitch{position:relative;min-height:620px;border-radius:18px;background:linear-gradient(90deg,#3f8b58,#4a9962);border:4px solid #fff;box-shadow:inset 0 0 0 1px rgba(255,255,255,.5),0 8px 24px rgba(42,92,55,.15);overflow:hidden}.prepPitch.favoriteOver{outline:6px solid #f3c44f;outline-offset:2px}.pitchLine{position:absolute;pointer-events:none;border:2px solid rgba(255,255,255,.88);z-index:0}.pitchHalf{left:0;right:0;top:50%;border-width:2px 0 0}.pitchCircle{width:118px;height:118px;border-radius:50%;left:50%;top:50%;transform:translate(-50%,-50%)}.pitchSpot{width:8px;height:8px;border-radius:50%;background:#fff;border:0;left:50%;top:50%;transform:translate(-50%,-50%)}.pitchBoxTop,.pitchBoxBottom{width:62%;height:18%;left:19%}.pitchBoxTop{top:-2px}.pitchBoxBottom{bottom:-2px}.pitchGoalTop,.pitchGoalBottom{width:30%;height:8%;left:35%}.pitchGoalTop{top:-2px}.pitchGoalBottom{bottom:-2px}.pitchArcTop,.pitchArcBottom{width:90px;height:45px;left:50%;transform:translateX(-50%);border-radius:0 0 90px 90px;border-top:0}.pitchArcTop{top:18%}.pitchArcBottom{bottom:18%;transform:translateX(-50%) rotate(180deg)}
    .prepSlot{position:absolute;width:128px;min-height:82px;transform:translate(-50%,-50%);background:rgba(255,255,255,.95);border:2px solid rgba(75,38,114,.18);border-radius:13px;padding:7px;text-align:center;box-shadow:0 3px 10px rgba(0,0,0,.14);z-index:2;transition:.15s}.prepSlot.empty{background:rgba(255,255,255,.30);border:2px dashed rgba(255,255,255,.95);color:#fff}.prepSlot.dragOver{outline:4px solid #f3c44f;transform:translate(-50%,-50%) scale(1.04)}.prepSlotPos{font-size:10px;color:#4b2672;font-weight:900}.prepSlot.empty .prepSlotPos{color:#fff}.prepSlotName{font-size:12px;font-weight:900;margin-top:3px}.prepSlotMeta{font-size:9px;color:#6b5877;margin-top:3px}.prepSlot.empty .prepSlotMeta{color:#fff}.prepSlot[draggable=true]{cursor:grab}
    .prepBenchDrop{border:2px dashed #ceb8e1;border-radius:12px;padding:8px;min-height:74px;background:#faf7fd}.prepBenchDrop.dragOver{background:#f0e5fa;border-color:#6f42c1}.prepBench{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:6px}.prepBenchCard{border:1px solid #dfd0ec;border-radius:10px;padding:7px;background:#fff;cursor:grab}.prepQuick{display:flex;gap:6px;flex-wrap:wrap;margin:8px 0}.prepQuick button{font-size:11px}.prepRoster{overflow:auto;max-height:620px;border:1px solid #eadff5;border-radius:12px;scroll-behavior:auto}.prepRoster table{width:100%;border-collapse:collapse;min-width:650px}.prepRoster th{position:sticky;top:0;background:#f8f4fc;z-index:4;padding:7px 5px;font-size:10px}.prepRoster td{padding:7px 5px}.prepRoster tr{border-top:1px solid #eee}.prepRoster tbody tr{cursor:grab}.prepRoster tbody tr:hover{background:#fcf9ff}.prepSummary{margin:10px 0;padding:9px;border-radius:10px;background:#f8f4fc}.prepSummary.ok{background:#eaf7ee;border:1px solid #9ed5ad}.prepSummary.bad{background:#fff4f4;border:1px solid #e5b0b0}.prepSectionLabel{font-size:11px;font-weight:900;color:#4b2672;text-transform:uppercase;letter-spacing:.04em;margin:9px 0 6px}.prepHint{font-size:10px;color:#6b5877;margin:5px 0 8px}.posFull{font-size:9px;color:#6b5877;font-weight:500;display:block;margin-top:1px}.prepRequired input:invalid{border:2px solid #b23a48;background:#fff7f7}.prepRequired small{display:block;color:#8b1f1f;margin-top:3px;font-size:9px}
    .prepFavoriteBox{border:2px solid #e3cf9c;background:#fffaf0;border-radius:14px;padding:10px;margin:10px 0}.prepFavoriteHead{display:flex;justify-content:space-between;gap:10px;align-items:end;flex-wrap:wrap}.prepFavoriteSave{display:grid;grid-template-columns:minmax(160px,240px) auto;gap:6px;align-items:end}.prepFavorites{display:flex;gap:7px;overflow-x:auto;padding:8px 0 2px;scrollbar-width:thin}.prepFavoriteCard{flex:0 0 210px;border:2px solid #d6b865;border-radius:12px;background:#fff;padding:9px;cursor:grab;box-shadow:0 2px 8px rgba(116,87,20,.08)}.prepFavoriteCard:hover{border-color:#9a7416}.prepFavoriteCard b,.prepFavoriteCard small{display:block}.prepFavoriteCard small{font-size:9px;color:#75613a;margin-top:3px}.prepFavoriteActions{display:flex;gap:5px;margin-top:7px}.prepFavoriteActions button{flex:1;padding:6px!important;font-size:10px!important}.prepFavoriteDelete{background:#fff!important;color:#983263!important;border:1px solid #c76a8d!important}
    @media(max-width:900px){.prepWorkspace{grid-template-columns:1fr}.prepPitch{min-height:570px}}@media(max-width:560px){.prepBench{grid-template-columns:1fr}.prepPitch{min-height:540px}.prepSlot{width:104px;min-height:72px;padding:5px}.prepSlotName{font-size:10px}.prepSlotPos{font-size:9px}.prepSlotMeta{display:none}.prepFavoriteSave{grid-template-columns:1fr}.prepFavoriteCard{flex-basis:190px}}
  `;doc.head.appendChild(s)}
  function ensureMounted(){if(panel)return panel;ensureStyles();const app=doc.getElementById('appPanel');if(!app)throw new Error('ClubMatch-appscherm ontbreekt');panel=doc.createElement('section');panel.id='v08Preparation';panel.className='card';const first=app.firstElementChild;first?.after(panel);if(!first)app.prepend(panel);return panel}
  function teamOptions(state){return '<option value="">— team/seizoen —</option>'+state.teamSeasons.map(t=>`<option value="${esc(t.team_season_id)}"${t.team_season_id===state.meta.teamSeasonId?' selected':''}>${esc(t.club_name)} · ${esc(t.team_name)} · ${esc(t.season_name)}</option>`).join('')}
  function formationOptions(state){const formations=global.ClubMatchV08Formation?.FORMATIONS||{'4-3-3':[]};return Object.keys(formations).map(code=>`<option value="${esc(code)}"${code===state.meta.formationCode?' selected':''}>${esc(code)}</option>`).join('')}
  function positionOptions(player,state){const slots=global.ClubMatchV08Formation?.getFormation?.(state.meta.formationCode)?.slots||[];return '<option value="">— positie —</option>'+slots.map(pos=>`<option value="${esc(pos)}"${pos===player.position?' selected':''}>${esc(global.ClubMatchV08Formation.slotLabel(pos))}</option>`).join('')}
  function playerLabel(player){return `#${esc(player.shirtNumber??'—')} ${esc(player.name)}`}
  function fieldLines(){return '<i class="pitchLine pitchHalf"></i><i class="pitchLine pitchCircle"></i><i class="pitchLine pitchSpot"></i><i class="pitchLine pitchBoxTop"></i><i class="pitchLine pitchBoxBottom"></i><i class="pitchLine pitchGoalTop"></i><i class="pitchLine pitchGoalBottom"></i><i class="pitchLine pitchArcTop"></i><i class="pitchLine pitchArcBottom"></i>'}
  function pitchHtml(state){if(!state.setup)return '<div class="muted">Kies eerst een team/seizoen.</div>';const geom=layout()?.geometry?.(state.meta.formationCode);if(!geom)return '<div class="muted">Veldmodel kon niet worden geladen.</div>';const byPos=new Map(state.players.filter(p=>p.starter&&p.position).map(p=>[String(p.position),p]));const slots=geom.slots.map(slot=>{const p=byPos.get(slot.position),style=`left:${slot.x}%;top:${slot.y}%`;return p?`<div class="prepSlot" draggable="true" data-player-id="${esc(p.playerId)}" data-position="${esc(slot.position)}" style="${style}"><div class="prepSlotPos">${esc(slot.position)}<span class="posFull">${esc(slot.label)}</span></div><div class="prepSlotName">${playerLabel(p)}</div><div class="prepSlotMeta">${esc((p.preferredPositions||[]).join(' · ')||'geen voorkeur')}</div></div>`:`<div class="prepSlot empty" data-position="${esc(slot.position)}" style="${style}"><div class="prepSlotPos">${esc(slot.position)}<span class="posFull">${esc(slot.label)}</span></div><div class="prepSlotName">Vrij</div><div class="prepSlotMeta">Sleep speler hierheen</div></div>`}).join('');return `<div class="prepPitch" data-prep-pitch>${fieldLines()}${slots}</div>`}
  function favoritesHtml(state){const list=state.favorites||[];return `<div class="prepFavoriteBox"><div class="prepFavoriteHead"><div><b>⭐ Favorite basisopstellingen</b><div class="prepHint">Cloud-favorites werken op desktop en tablet. Klik Toepassen of sleep de favorite naar de visuele basisopstelling.</div></div><div class="prepFavoriteSave"><label>Naam favorite<input id="prepFavoriteName" maxlength="60" value="${esc(favoriteDraft)}" placeholder="Bijv. Sterkste 4-3-3"></label><button id="prepFavoriteSaveBtn" class="secondary" data-v08-action ${state.validation?.starterCount===11?'':'disabled'}>⭐ Huidige basis opslaan</button></div></div><div class="prepFavorites">${list.length?list.map(f=>`<article class="prepFavoriteCard" draggable="true" data-favorite-id="${esc(f.id)}"><b>⭐ ${esc(f.name)}</b><small>${esc(f.formationCode)} · 11 spelers · Cloud</small><div class="prepFavoriteActions"><button type="button" class="secondary" data-favorite-apply="${esc(f.id)}">Toepassen</button><button type="button" class="prepFavoriteDelete" data-favorite-delete="${esc(f.id)}">Verwijder</button></div></article>`).join(''):'<div class="muted">Nog geen favorites voor dit team/seizoen.</div>'}</div></div>`}
  function benchHtml(state){const bench=state.players.filter(p=>p.selected&&!p.starter);return `<div class="prepBenchDrop" data-prep-bench-drop>${bench.length?`<div class="prepBench">${bench.map(p=>`<div class="prepBenchCard" draggable="true" data-player-id="${esc(p.playerId)}"><b>${playerLabel(p)}</b><div class="muted">${esc((p.preferredPositions||[]).join(' · ')||'geen voorkeurspositie')}</div></div>`).join('')}</div>`:'<div class="muted">Nog geen geselecteerde reserves. Sleep een basisspeler hierheen om hem op de bank te zetten.</div>'}</div>`}
  function rosterHtml(state){if(!state.setup)return '<div class="muted">Kies eerst een team/seizoen.</div>';const players=[...state.players].sort((a,b)=>(Number(a.shirtNumber)||999)-(Number(b.shirtNumber)||999)||String(a.name).localeCompare(String(b.name)));return `<div class="prepRoster"><table><thead><tr><th style="text-align:left">Speler</th><th>Aanwezig</th><th>Selectie</th><th>Basis</th><th style="text-align:left">Positie in ${esc(state.meta.formationCode)}</th></tr></thead><tbody>${players.map(player=>`<tr draggable="${player.attendance?'true':'false'}" data-prep-player="${esc(player.playerId)}"><td><b>${playerLabel(player)}</b><div class="muted">Voorkeur: ${esc((player.preferredPositions||[]).join(' · ')||'—')}</div></td><td style="text-align:center"><input type="checkbox" data-prep-field="attendance" ${player.attendance?'checked':''}></td><td style="text-align:center"><input type="checkbox" data-prep-field="selected" ${player.selected?'checked':''} ${!player.attendance?'disabled':''}></td><td style="text-align:center"><input type="checkbox" data-prep-field="starter" ${player.starter?'checked':''} ${!player.attendance?'disabled':''}></td><td><select data-prep-field="position" ${!player.starter?'disabled':''}>${positionOptions(player,state)}</select></td></tr>`).join('')}</tbody></table></div>`}
  function render(state){lastState=state;const root=ensureMounted(),oldRoster=root.querySelector('.prepRoster'),scrollTop=oldRoster?.scrollTop||0,scrollLeft=oldRoster?.scrollLeft||0;const oldFav=root.querySelector('#prepFavoriteName');if(oldFav)favoriteDraft=oldFav.value||favoriteDraft;const v=state.validation||{errors:[],selectedCount:0,starterCount:0,benchCount:0,attendanceCount:0,ok:false};const ready=v.ok&&!state.dirty&&!!state.meta.matchId;root.innerHTML=`<div class="sectionTitle"><div><h2 style="margin-bottom:3px">Wedstrijdvoorbereiding</h2><div class="muted">Selectie, basisopstelling, formatie, bank en favorite basisopstellingen uit de Cloud. Refresh mag niets verliezen.</div></div><div class="controls"><button id="prepNewBtn" class="secondary" data-v08-action>Nieuwe voorbereiding</button><button id="prepCloseBtn" class="secondary" data-v08-action>Sluiten</button></div></div>
      <div class="grid3" style="margin-top:10px"><label>Team / seizoen<select id="prepTeam">${teamOptions(state)}</select></label><label class="prepRequired">Tegenstander *<input id="prepOpponent" required value="${esc(state.meta.opponentName)}" placeholder="Verplicht vóór starten"><small>Zonder tegenstander kan de wedstrijd niet starten.</small></label><label>Formatie<select id="prepFormation">${formationOptions(state)}</select></label></div>
      <div class="grid3" style="margin-top:8px"><label>Datum<input id="prepDate" type="date" value="${esc(state.meta.matchDate)}"></label><label>Tijd<input id="prepTime" type="time" value="${esc(state.meta.scheduledTime)}"></label><label>Wedstrijdduur (min)<input id="prepDuration" type="number" min="1" max="180" value="${esc(state.meta.officialDurationMinutes)}"></label></div>
      <div class="prepQuick"><button id="prepSelectPresentBtn" class="secondary" data-v08-action>✓ Alle aanwezigen selecteren</button><button id="prepFirstElevenBtn" class="secondary" data-v08-action>11 eerste geselecteerden → basis</button><button id="prepApplyFormationBtn" class="secondary" data-v08-action>⚽ Formatie automatisch indelen</button><button id="prepClearSelectionBtn" class="secondary" data-v08-action>Selectie leegmaken</button></div>
      <div class="prepSummary ${v.ok?'ok':'bad'}"><b>${v.attendanceCount} aanwezig · ${v.selectedCount} selectie · ${v.starterCount} basis · ${v.benchCount} bank</b> · ${state.dirty?'nog niet opgeslagen':'opgeslagen'}${state.meta.matchId?` · wedstrijd ${esc(state.meta.matchId).slice(0,8)}…`:''}<div class="muted" style="margin-top:4px">${v.errors.length?esc(v.errors.join(' · ')):'✓ Voorbereiding compleet en startklaar.'}</div></div>
      ${state.setup?favoritesHtml(state):''}
      <div class="prepWorkspace"><div><div class="prepSectionLabel">Visuele basisopstelling · ${esc(state.meta.formationCode)}</div><div class="prepHint">Sleep een aanwezige speler uit de lijst naar het veld, of sleep een favorite basisopstelling op het veld om alle 11 posities in één keer toe te passen.</div>${pitchHtml(state)}<div class="prepSectionLabel">Bank / reserves</div>${benchHtml(state)}</div><div><div class="prepSectionLabel">Beschikbare spelers en selectie</div>${rosterHtml(state)}</div></div>
      <div class="controls" style="margin-top:12px"><button id="prepSaveBtn" data-v08-action ${!state.setup?'disabled':''}>Voorbereiding opslaan</button><button id="prepStartBtn" data-v08-action ${!ready?'disabled':''}>▶ Wedstrijd starten</button></div>`;bind(root);const newRoster=root.querySelector('.prepRoster');if(newRoster){newRoster.scrollTop=scrollTop;newRoster.scrollLeft=scrollLeft}root.classList.remove('hidden');return root}
  function syncMeta(){controller.setMeta({opponentName:doc.getElementById('prepOpponent')?.value||'',matchDate:doc.getElementById('prepDate')?.value||'',scheduledTime:doc.getElementById('prepTime')?.value||'',officialDurationMinutes:Number(doc.getElementById('prepDuration')?.value||80)})}
  function dragStart(event,playerId){dragFavoriteId='';dragPlayerId=playerId;event.dataTransfer?.setData('text/plain',playerId);event.dataTransfer?.setData('application/x-clubmatch-kind','player');if(event.dataTransfer)event.dataTransfer.effectAllowed='move'}
  function dragFavoriteStart(event,favoriteId){dragPlayerId='';dragFavoriteId=favoriteId;event.dataTransfer?.setData('application/x-clubmatch-favorite',favoriteId);event.dataTransfer?.setData('application/x-clubmatch-kind','favorite');if(event.dataTransfer)event.dataTransfer.effectAllowed='copy'}
  function dragPlayer(event){if(event.dataTransfer?.getData('application/x-clubmatch-kind')==='favorite')return '';return event.dataTransfer?.getData('text/plain')||dragPlayerId}
  function draggedFavorite(event){return event.dataTransfer?.getData('application/x-clubmatch-favorite')||dragFavoriteId}
  function applyFavoriteDrop(event,root){const favoriteId=draggedFavorite(event);if(!favoriteId)return false;event.preventDefault();event.stopPropagation();root.querySelector('[data-prep-pitch]')?.classList.remove('favoriteOver');options.run?.('Favorite basisopstelling toepassen',()=>controller.applyFavorite(favoriteId));dragFavoriteId='';return true}
  function dragTarget(root){root.querySelectorAll('.prepSlot').forEach(slot=>{slot.addEventListener('dragover',e=>{e.preventDefault();if(draggedFavorite(e)){root.querySelector('[data-prep-pitch]')?.classList.add('favoriteOver');return}slot.classList.add('dragOver')});slot.addEventListener('dragleave',()=>slot.classList.remove('dragOver'));slot.addEventListener('drop',e=>{if(applyFavoriteDrop(e,root))return;e.preventDefault();slot.classList.remove('dragOver');const id=dragPlayer(e);if(id)options.run?.('Basisopstelling wijzigen',()=>controller.placeStarter(id,slot.dataset.position))})});const pitch=root.querySelector('[data-prep-pitch]');pitch?.addEventListener('dragover',e=>{if(!draggedFavorite(e))return;e.preventDefault();pitch.classList.add('favoriteOver')});pitch?.addEventListener('dragleave',e=>{if(e.target===pitch)pitch.classList.remove('favoriteOver')});pitch?.addEventListener('drop',e=>applyFavoriteDrop(e,root));const bench=root.querySelector('[data-prep-bench-drop]');bench?.addEventListener('dragover',e=>{if(draggedFavorite(e))return;e.preventDefault();bench.classList.add('dragOver')});bench?.addEventListener('dragleave',()=>bench.classList.remove('dragOver'));bench?.addEventListener('drop',e=>{if(draggedFavorite(e))return;e.preventDefault();bench.classList.remove('dragOver');const id=dragPlayer(e);if(id)options.run?.('Speler naar bank',()=>controller.moveToBench(id))})}
  function bind(root){const team=root.querySelector('#prepTeam');if(team)team.onchange=()=>options.run?.('Team laden',()=>controller.loadTeam(team.value));['prepOpponent','prepDate','prepTime','prepDuration'].forEach(id=>{const el=root.querySelector(`#${id}`);if(el)el.onchange=syncMeta});const formation=root.querySelector('#prepFormation');if(formation)formation.onchange=()=>controller.setFormation(formation.value);root.querySelectorAll('[data-prep-player]').forEach(row=>{row.addEventListener('dragstart',e=>dragStart(e,row.dataset.prepPlayer));row.querySelectorAll('[data-prep-field]').forEach(input=>input.onchange=()=>{const field=input.dataset.prepField,value=field==='position'?input.value:!!input.checked;controller.patchPlayer(row.dataset.prepPlayer,{[field]:value})})});root.querySelectorAll('.prepSlot[data-player-id],.prepBenchCard').forEach(node=>node.addEventListener('dragstart',e=>dragStart(e,node.dataset.playerId)));root.querySelectorAll('[data-favorite-id]').forEach(card=>card.addEventListener('dragstart',e=>dragFavoriteStart(e,card.dataset.favoriteId)));root.querySelectorAll('[data-favorite-apply]').forEach(btn=>btn.onclick=()=>options.run?.('Favorite basisopstelling toepassen',()=>controller.applyFavorite(btn.dataset.favoriteApply)));root.querySelectorAll('[data-favorite-delete]').forEach(btn=>btn.onclick=e=>{e.stopPropagation();if(global.confirm?.('Deze favorite basisopstelling verwijderen?'))options.run?.('Favorite verwijderen',()=>controller.deleteFavorite(btn.dataset.favoriteDelete))});const favInput=root.querySelector('#prepFavoriteName');if(favInput)favInput.oninput=()=>{favoriteDraft=favInput.value};root.querySelector('#prepFavoriteSaveBtn')?.addEventListener('click',()=>options.run?.('Favorite basisopstelling opslaan',async()=>{favoriteDraft=root.querySelector('#prepFavoriteName')?.value||'';const state=await controller.saveFavorite(favoriteDraft);favoriteDraft='';return state}));dragTarget(root);root.querySelector('#prepSelectPresentBtn')?.addEventListener('click',()=>controller.selectAllPresent());root.querySelector('#prepClearSelectionBtn')?.addEventListener('click',()=>controller.clearSelection());root.querySelector('#prepFirstElevenBtn')?.addEventListener('click',()=>options.run?.('Basisspelers kiezen',()=>controller.makeSelectedStarters()));root.querySelector('#prepApplyFormationBtn')?.addEventListener('click',()=>options.run?.('Formatie toepassen',()=>controller.applyFormation(root.querySelector('#prepFormation')?.value||'4-3-3')));root.querySelector('#prepSaveBtn')?.addEventListener('click',()=>options.run?.('Voorbereiding opslaan',async()=>{syncMeta();const snap=await controller.save();await options.onSaved?.(snap);return snap}));root.querySelector('#prepStartBtn')?.addEventListener('click',()=>options.run?.('Wedstrijd starten',async()=>{syncMeta();const id=controller.state.meta.matchId;const result=await controller.start();await options.onStarted?.(id,result);return result}));root.querySelector('#prepNewBtn')?.addEventListener('click',()=>{favoriteDraft='';controller.clear();render(controller.state)});root.querySelector('#prepCloseBtn')?.addEventListener('click',hide)}
  function hide(){ensureMounted().classList.add('hidden')}function show(){ensureMounted().classList.remove('hidden');if(lastState)render(lastState)}async function openExisting(match){const state=await controller.openExisting(match);return render(state)}function renderCurrent(){return render(controller.state)}
  return Object.freeze({render,renderCurrent,openExisting,show,hide,get visible(){return panel&&!panel.classList.contains('hidden')}})
}
global.ClubMatchV08PreparationUi={createPreparationUi};
})(typeof window!=='undefined'?window:globalThis);
