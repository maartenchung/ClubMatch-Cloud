/* ClubMatch Cloud v0.8 - confirmed-state mutation controller with single-flight protection */
(function(global){
'use strict';
function invariant(condition,message,code){if(condition)return;const error=new Error(message);error.code=code||'CLUBMATCH_VALIDATION';throw error}
function defaultIdFactory(){if(global.crypto?.randomUUID)return global.crypto.randomUUID();return `cm-${Date.now().toString(36)}-${Math.random().toString(36).slice(2)}`}
function unwrapRpc(result,name){if(result?.error){const error=result.error instanceof Error?result.error:new Error(result.error.message||String(result.error));error.rpc=name;throw error}return Object.prototype.hasOwnProperty.call(result||{},'data')?result.data:result}
function exactTime(minute,second=0){const m=Number(minute),s=Number(second);invariant(Number.isInteger(m)&&m>=0&&m<=300,'Wedstrijdminuut moet tussen 0 en 300 liggen');invariant(Number.isInteger(s)&&s>=0&&s<=59,'Wedstrijdseconde moet tussen 0 en 59 liggen');return {minute:m,second:s}}
function findBaseEvent(snapshot,eventId,types){const allowed=Array.isArray(types)?types:[types];return (snapshot?.events||[]).find(e=>e.id===eventId&&allowed.includes(e.event_type))}
function isVoided(snapshot,eventId){return (snapshot?.events||[]).some(e=>/_voided$/.test(e.event_type)&&e.target_event_id===eventId)}
function selectedPlayerIds(snapshot){return new Set((snapshot?.players||[]).filter(p=>p.selected).map(p=>p.player_id))}
function normalizedAssignments(assignments=[]){return assignments.map(item=>({player_id:item.player_id||item.playerId,position:String(item.position||'').trim()}))}
function stateVersion(snapshot){return Math.max(0,Number(snapshot?.state?.state_version)||0)}
function createConflictError(action,beforeVersion,afterVersion,cause){const error=new Error('Actie niet uitgevoerd: de wedstrijdstatus is intussen op een ander apparaat gewijzigd. De nieuwste Cloud-status is geladen; controleer de actuele situatie en voer de actie zo nodig opnieuw in.');error.code='CLUBMATCH_CONFLICT';error.action=action;error.beforeVersion=beforeVersion;error.afterVersion=afterVersion;error.cause=cause;return error}
function createMutationController(options){
  invariant(options&&typeof options.rpc==='function','RPC is verplicht','CLUBMATCH_CONFIG');invariant(typeof options.deriveSnapshot==='function','Snapshot-afleiding is verplicht','CLUBMATCH_CONFIG');
  const rpc=options.rpc,deriveSnapshot=options.deriveSnapshot,renderConfirmed=typeof options.renderConfirmed==='function'?options.renderConfirmed:()=>{},idFactory=options.idFactory||defaultIdFactory;
  let queue=Promise.resolve(),pendingCount=0;const inFlight=new Map();
  async function callRpc(name,params){return unwrapRpc(await rpc(name,params),name)}
  async function readConfirmed(matchId){invariant(matchId,'Wedstrijd-ID is verplicht');const snapshot=await callRpc('get_match_snapshot',{p_match_id:matchId});invariant(snapshot?.match&&snapshot?.state,'Bevestigde wedstrijdsnapshot is onvolledig','CLUBMATCH_SNAPSHOT');const liveState=deriveSnapshot(snapshot);invariant(liveState&&liveState.players,'Afgeleide live-status is onvolledig','CLUBMATCH_SNAPSHOT');return {snapshot,liveState}}
  function enqueue(task){pendingCount++;const run=queue.then(task,task);queue=run.catch(()=>{}).finally(()=>{pendingCount--});return run}
  function singleFlight(key,task){if(!key)return task();const existing=inFlight.get(key);if(existing)return existing;const promise=Promise.resolve().then(task).finally(()=>{if(inFlight.get(key)===promise)inFlight.delete(key)});inFlight.set(key,promise);return promise}
  async function recoverConcurrentFailure(spec,before,clientEventId,cause){let latest;try{latest=await readConfirmed(spec.matchId)}catch{return null}const beforeVersion=stateVersion(before.snapshot),afterVersion=stateVersion(latest.snapshot);if(afterVersion<=beforeVersion)return null;renderConfirmed(latest.snapshot,latest.liveState,{action:spec.action,clientEventId,conflict:true,source:'conflict-recovery',beforeVersion,afterVersion,mutationError:cause?.message||String(cause||'')});return createConflictError(spec.action,beforeVersion,afterVersion,cause)}
  function execute(spec){const key=spec.dedupeKey||null;return singleFlight(key,()=>enqueue(async()=>{const before=await readConfirmed(spec.matchId),clientEventId=spec.clientEventId||idFactory();invariant(clientEventId,'client_event_id kon niet worden aangemaakt','CLUBMATCH_CLIENT_EVENT_ID');const mutation=spec.build(before.liveState,clientEventId,before.snapshot);invariant(mutation?.rpc&&mutation?.params,'Mutatiespecificatie is onvolledig','CLUBMATCH_CONFIG');let mutationResult;try{mutationResult=await callRpc(mutation.rpc,mutation.params)}catch(error){const conflict=await recoverConcurrentFailure(spec,before,clientEventId,error);if(conflict)throw conflict;throw error}const after=await readConfirmed(spec.matchId),beforeVersion=stateVersion(before.snapshot),afterVersion=stateVersion(after.snapshot);invariant(afterVersion>=beforeVersion,'Bevestigde statusversie ging achteruit','CLUBMATCH_STATE_VERSION');renderConfirmed(after.snapshot,after.liveState,{action:spec.action,clientEventId,mutationResult,beforeVersion,afterVersion});return {action:spec.action,clientEventId,mutationResult,before,after}}))}
  function substitute(input){return execute({action:'SUBSTITUTION',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`SUB|${input.matchId}|${input.outId}|${input.inId}`,build(state,clientEventId){const out=state.players[input.outId],inn=state.players[input.inId];invariant(out&&inn&&input.outId!==input.inId,'Kies twee verschillende wedstrijdspelers');invariant(out.currentRole==='FIELD','Uitgaande speler staat niet op het bevestigde veld');invariant(inn.currentRole==='BENCH','Inkomende speler staat niet op de bevestigde bank');const position=(input.position||out.currentPosition||'').trim();invariant(position,'Positie van de inkomende speler is verplicht');return {rpc:'record_substitution',params:{p_match_id:input.matchId,p_player_out_id:input.outId,p_player_in_id:input.inId,p_new_position:position,p_client_event_id:clientEventId}}}})}
  function changePosition(input){return execute({action:'POSITION_CHANGED',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`POS|${input.matchId}|${input.playerId}|${String(input.position||'').trim()}`,build(state,clientEventId){const player=state.players[input.playerId],position=String(input.position||'').trim();invariant(player?.currentRole==='FIELD','Speler staat niet op het bevestigde veld');invariant(position,'Nieuwe positie is verplicht');invariant(position!==player.currentPosition,'Speler staat al op deze positie');const occupied=state.fieldIds.find(id=>id!==input.playerId&&state.players[id].currentPosition===position);invariant(!occupied,'Positie is bezet; gebruik posities ruilen','CLUBMATCH_POSITION_OCCUPIED');return {rpc:'change_player_position',params:{p_match_id:input.matchId,p_player_id:input.playerId,p_new_position:position,p_client_event_id:clientEventId}}}})}
  function swapPositions(input){return execute({action:'POSITIONS_SWAPPED',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`SWAP|${input.matchId}|${[input.playerId,input.otherPlayerId].sort().join('|')}`,build(state,clientEventId){const first=state.players[input.playerId],second=state.players[input.otherPlayerId];invariant(first&&second&&input.playerId!==input.otherPlayerId,'Kies twee verschillende wedstrijdspelers');invariant(first.currentRole==='FIELD'&&second.currentRole==='FIELD','Beide spelers moeten op het bevestigde veld staan');invariant(first.currentPosition&&second.currentPosition,'Beide spelers hebben een bevestigde positie nodig');return {rpc:'swap_player_positions',params:{p_match_id:input.matchId,p_player_a_id:input.playerId,p_player_b_id:input.otherPlayerId,p_client_event_id:clientEventId}}}})}
  function changeFormation(input){return execute({action:'FORMATION_CHANGED',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`FORM|${input.matchId}|${input.formationCode}`,build(state,clientEventId,snapshot){const formationCode=String(input.formationCode||'').trim(),assignments=normalizedAssignments(input.assignments||[]);invariant(formationCode,'Formatie is verplicht');invariant(['live','halftime'].includes(snapshot?.match?.status),'Formatie wijzigen kan alleen tijdens een live wedstrijd of rust');invariant(assignments.length===11,'Formatie vereist exact 11 veldspelers');const ids=assignments.map(a=>a.player_id),positions=assignments.map(a=>a.position);invariant(ids.every(Boolean)&&new Set(ids).size===11,'Formatie vereist 11 unieke spelers');invariant(positions.every(Boolean)&&new Set(positions).size===11,'Formatie vereist 11 unieke posities');const field=new Set(state.fieldIds);invariant(ids.every(id=>field.has(id))&&state.fieldIds.every(id=>ids.includes(id)),'Formatie moet exact de huidige 11 veldspelers bevatten');const hasSub=(snapshot?.events||[]).some(e=>e.event_type==='substitution');if(input.updateBasis===true)invariant(!hasSub,'Basisopstelling kan niet meer worden aangepast na de eerste wissel');return {rpc:'change_match_formation_v08',params:{p_match_id:input.matchId,p_formation_code:formationCode,p_assignments:assignments,p_client_event_id:clientEventId,p_update_basis:input.updateBasis===true}}}})}
  function recordGoal(input){return execute({action:'GOAL',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`GOAL|${input.matchId}|${input.side}`,build(state,clientEventId){invariant(input.side==='for'||input.side==='against','Doelpuntzijde moet eigen team of tegenstander zijn');if(input.side==='for'){invariant(state.players[input.scorerId]?.currentRole==='FIELD','Doelpuntenmaker staat niet op het bevestigde veld');if(input.assistId){invariant(input.assistId!==input.scorerId,'Doelpuntenmaker en assist mogen niet dezelfde speler zijn');invariant(state.players[input.assistId]?.currentRole==='FIELD','Assistspeler staat niet op het bevestigde veld')}}else invariant(!input.scorerId&&!input.assistId,'Doelpunt tegenstander gebruikt geen ClubMatch-spelers');return {rpc:'record_goal',params:{p_match_id:input.matchId,p_side:input.side,p_client_event_id:clientEventId,p_scorer_player_id:input.scorerId||null,p_assist_player_id:input.assistId||null,p_goal_type:input.goalType||null,p_note:input.note||null}}}})}
  function recordPenalty(input){return execute({action:'REGULAR_PENALTY',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`PENALTY|${input.matchId}|${input.side||'for'}`,build(state,clientEventId){const side=input.side==='against'?'against':'for',outcome=String(input.outcome||'').trim();invariant(['scored','missed','saved'].includes(outcome),'Kies raak, gemist of gestopt');if(side==='for'){invariant(state.players[input.takerId]?.currentRole==='FIELD','Penaltynemer staat niet op het bevestigde veld');if(input.fouledPlayerId)invariant(state.players[input.fouledPlayerId],'Speler waarop de overtreding is gemaakt ontbreekt')}return {rpc:'record_match_penalty_v08',params:{p_match_id:input.matchId,p_side:side,p_outcome:outcome,p_client_event_id:clientEventId,p_taker_player_id:side==='for'?input.takerId:null,p_fouled_player_id:side==='for'?(input.fouledPlayerId||null):null,p_match_minute:null,p_match_second:null,p_note:String(input.note||'').trim()||null}}}})}
  function correctSubstitution(input){return execute({action:'SUBSTITUTION_CORRECTED',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`CSUB|${input.matchId}|${input.eventId}`,build(_state,clientEventId,snapshot){const original=findBaseEvent(snapshot,input.eventId,'substitution');invariant(original,'Originele wissel niet gevonden');invariant(!isVoided(snapshot,input.eventId),'Deze wissel is al ongeldig gemaakt');const selected=selectedPlayerIds(snapshot);invariant(selected.has(input.outId)&&selected.has(input.inId)&&input.outId!==input.inId,'Kies twee verschillende spelers uit de wedstrijdselectie');const position=String(input.position||'').trim();invariant(position,'Nieuwe positie is verplicht');const time=exactTime(input.matchMinute,input.matchSecond);return {rpc:'correct_substitution',params:{p_match_id:input.matchId,p_substitution_event_id:input.eventId,p_client_event_id:clientEventId,p_player_out_id:input.outId,p_player_in_id:input.inId,p_new_position:position,p_match_minute:time.minute,p_match_second:time.second}}}})}
  function voidSubstitution(input){return execute({action:'SUBSTITUTION_VOIDED',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`VSUB|${input.matchId}|${input.eventId}`,build(_state,clientEventId,snapshot){invariant(findBaseEvent(snapshot,input.eventId,'substitution'),'Originele wissel niet gevonden');invariant(!isVoided(snapshot,input.eventId),'Deze wissel is al ongeldig gemaakt');return {rpc:'void_substitution',params:{p_match_id:input.matchId,p_substitution_event_id:input.eventId,p_client_event_id:clientEventId,p_reason:String(input.reason||'').trim()||null}}}})}
  function correctPositionChange(input){return execute({action:'POSITION_CORRECTED',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`CPOS|${input.matchId}|${input.eventId}`,build(_state,clientEventId,snapshot){invariant(findBaseEvent(snapshot,input.eventId,'position_changed'),'Originele positiewijziging niet gevonden');invariant(!isVoided(snapshot,input.eventId),'Deze positiewijziging is al ongeldig gemaakt');const selected=selectedPlayerIds(snapshot);invariant(selected.has(input.playerId),'Speler hoort niet bij de wedstrijdselectie');const position=String(input.position||'').trim();invariant(position,'Nieuwe positie is verplicht');const time=exactTime(input.matchMinute,input.matchSecond);return {rpc:'correct_position_change',params:{p_match_id:input.matchId,p_position_event_id:input.eventId,p_client_event_id:clientEventId,p_player_id:input.playerId,p_new_position:position,p_match_minute:time.minute,p_match_second:time.second}}}})}
  function voidPositionChange(input){return execute({action:'POSITION_VOIDED',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`VPOS|${input.matchId}|${input.eventId}`,build(_state,clientEventId,snapshot){invariant(findBaseEvent(snapshot,input.eventId,'position_changed'),'Originele positiewijziging niet gevonden');invariant(!isVoided(snapshot,input.eventId),'Deze positiewijziging is al ongeldig gemaakt');return {rpc:'void_position_change',params:{p_match_id:input.matchId,p_position_event_id:input.eventId,p_client_event_id:clientEventId,p_reason:String(input.reason||'').trim()||null}}}})}
  function correctGoal(input){return execute({action:'GOAL_CORRECTED',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`CGOAL|${input.matchId}|${input.eventId}`,build(_state,clientEventId,snapshot){const original=findBaseEvent(snapshot,input.eventId,['goal_for','goal_against']);invariant(original,'Origineel doelpunt niet gevonden');invariant(!isVoided(snapshot,input.eventId),'Dit doelpunt is al ongeldig gemaakt');const side=original.goal?.side||(original.event_type==='goal_for'?'for':'against'),selected=selectedPlayerIds(snapshot);if(side==='for'){invariant(selected.has(input.scorerId),'Kies een doelpuntenmaker uit de wedstrijdselectie');if(input.assistId){invariant(input.assistId!==input.scorerId,'Doelpuntenmaker en assist mogen niet dezelfde speler zijn');invariant(selected.has(input.assistId),'Kies een assist uit de wedstrijdselectie')}}else invariant(!input.scorerId&&!input.assistId,'Doelpunt tegenstander gebruikt geen ClubMatch-spelers');const time=exactTime(input.matchMinute,input.matchSecond);return {rpc:'correct_goal',params:{p_match_id:input.matchId,p_goal_event_id:input.eventId,p_client_event_id:clientEventId,p_scorer_player_id:side==='for'?input.scorerId:null,p_assist_player_id:side==='for'?(input.assistId||null):null,p_goal_type:String(input.goalType||'').trim()||null,p_note:String(input.note||'').trim()||null,p_match_minute:time.minute,p_match_second:time.second}}}})}
  function voidGoal(input){return execute({action:'GOAL_VOIDED',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`VGOAL|${input.matchId}|${input.eventId}`,build(_state,clientEventId,snapshot){invariant(findBaseEvent(snapshot,input.eventId,['goal_for','goal_against']),'Origineel doelpunt niet gevonden');invariant(!isVoided(snapshot,input.eventId),'Dit doelpunt is al ongeldig gemaakt');return {rpc:'void_goal',params:{p_match_id:input.matchId,p_goal_event_id:input.eventId,p_client_event_id:clientEventId,p_reason:String(input.reason||'').trim()||null}}}})}
  function advanceClock(input){return execute({action:'CLOCK',matchId:input.matchId,clientEventId:input.clientEventId,dedupeKey:`CLOCK|${input.matchId}|${input.clockAction}`,build(_state,clientEventId,snapshot){const allowed=['pause','resume','halftime','second_half','injury_time','finish'];invariant(allowed.includes(input.clockAction),'Niet-ondersteunde klokactie');if(input.clockAction==='injury_time')invariant(Number.isInteger(input.minutes)&&input.minutes>=0&&input.minutes<=60,'Extra tijd moet tussen 0 en 60 minuten liggen');invariant(snapshot.match.status!=='finished','Wedstrijd is al gestopt');return {rpc:'advance_match_clock',params:{p_match_id:input.matchId,p_action:input.clockAction,p_client_event_id:clientEventId,p_minutes:input.clockAction==='injury_time'?input.minutes:null}}}})}
  function deleteMatch(input){return singleFlight(`DELETE|${input?.matchId}`,()=>enqueue(async()=>{invariant(input?.matchId,'Wedstrijd-ID is verplicht');invariant(input.confirmation==='DELETE','Expliciete DELETE-bevestiging is verplicht','CLUBMATCH_DELETE_CONFIRMATION');const before=await readConfirmed(input.matchId);invariant(!['live','halftime'].includes(before.snapshot.match.status),'Actieve wedstrijd moet eerst worden gestopt','CLUBMATCH_DELETE_ACTIVE');const mutationResult=await callRpc('delete_match_v08',{p_match_id:input.matchId,p_confirmation:input.confirmation});invariant(mutationResult?.ok===true,'Server bevestigde verwijderen niet','CLUBMATCH_DELETE_FAILED');return {action:'MATCH_DELETED',mutationResult,before,after:null}}))}
  return {readConfirmed,substitute,changePosition,swapPositions,changeFormation,recordGoal,recordPenalty,correctSubstitution,voidSubstitution,correctPositionChange,voidPositionChange,correctGoal,voidGoal,advanceClock,deleteMatch,get pendingCount(){return pendingCount},get pendingKeys(){return [...inFlight.keys()]}};
}
global.ClubMatchV08MutationController={createMutationController,exactTime,findBaseEvent,isVoided,normalizedAssignments,stateVersion,createConflictError};
})(typeof window!=='undefined'?window:globalThis);
