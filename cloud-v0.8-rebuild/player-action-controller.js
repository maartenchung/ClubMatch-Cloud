/* ClubMatch Cloud v0.8 - quick actions, possession and explicit analyst inference bundles */
(function(global){
'use strict';
const ACTIONS=Object.freeze(['possession_control','possession_start','possession_stop','pass_received','pass_completed','ball_recovery','interception','block','ball_loss','bad_pass','chance_created','duel_won','duel_lost','shot','shot_on_target','foul_committed','foul_won','injury','save']);
const LOSS_CAUSES=Object.freeze(['bad_pass','duel_lost','poor_control','dribble','interception','other','unknown']);
const ANALYST_ACTIONS=new Set(['possession_start','shot','shot_on_target','ball_recovery','interception','ball_loss','bad_pass','foul_won','foul_committed']);
function invariant(ok,message){if(!ok)throw new Error(message)}
function totalSecond(event){return Math.max(0,(Number(event?.match_minute)||0)*60+(Number(event?.match_second)||0))}
function eventOrder(a,b){return totalSecond(a)-totalSecond(b)||String(a?.occurred_at||a?.created_at||'').localeCompare(String(b?.occurred_at||b?.created_at||''))||String(a?.id||'').localeCompare(String(b?.id||''))}
function createPlayerActionController(options={}){
 const client=options.client,runtime=options.runtime;invariant(client?.rpc&&runtime,'Cloud-client en live-runtime zijn verplicht');let snapshot=null,queue=Promise.resolve();
 const id=()=>global.crypto?.randomUUID?.()||`action-${Date.now()}-${Math.random().toString(16).slice(2)}`;
 function setSnapshot(next){snapshot=next?.match?.id?next:null;return snapshot}
 function player(playerId){return (snapshot?.players||[]).find(p=>p.player_id===playerId)||null}
 function possession(){const events=(snapshot?.events||[]).filter(e=>e.event_type==='player_action'&&['possession_start','possession_stop'].includes(e?.payload?.action)).slice().sort(eventOrder);const last=events.at(-1);if(!last||last.payload?.action!=='possession_start')return null;const p=player(last.subject_player_id);return p?{playerId:p.player_id,name:p.display_name||p.full_name||p.player_id,shirtNumber:p.shirt_number??null,startedSecond:totalSecond(last)}:null}
 function teamPossession(){const events=(snapshot?.events||[]).filter(e=>e.event_type==='team_possession').slice().sort(eventOrder);const last=events.at(-1);if(!last||last.payload?.action!=='start')return null;return {side:last.payload?.side==='against'?'against':'for',period:last.payload?.period||snapshot?.state?.period||'',startedSecond:totalSecond(last)} }
 async function call(name,params){const result=await client.rpc(name,params);if(result?.error)throw result.error;await runtime.refresh(name);return result?.data}
 async function rawRecord(playerId,action,note='',context={}){invariant(snapshot?.match?.id&&runtime.activeMatchId,'Open eerst een live wedstrijd');invariant(ACTIONS.includes(action),'Onbekende speleractie');const p=player(playerId);invariant(p?.selected,'Speler hoort niet bij de wedstrijdselectie');if(!['injury','possession_stop'].includes(action))invariant(p.is_on_field,'Deze actie kan alleen voor een veldspeler');return call('record_player_action_v2',{p_match_id:runtime.activeMatchId,p_player_id:playerId,p_action:action,p_client_event_id:id(),p_related_player_id:context.relatedPlayerId||null,p_cause:context.cause||null,p_note:String(note||'').trim()||null})}
 function enqueue(fn){const next=queue.then(fn,fn);queue=next.catch(()=>{});return next}
 function record(playerId,action,note='',context={}){if(action==='bad_pass')return loseBall(playerId,'bad_pass',note);if(action==='ball_loss')return loseBall(playerId,context.cause||'unknown',note);return enqueue(()=>rawRecord(playerId,action,note,context))}
 function analystRecord(playerId,action,note='',context={}){return enqueue(async()=>{invariant(snapshot?.match?.id&&runtime.activeMatchId,'Open eerst een live wedstrijd');invariant(ACTIONS.includes(action),'Onbekende analistactie');const p=player(playerId);invariant(p?.selected&&p.is_on_field,'Analistactie vereist een veldspeler');if(!ANALYST_ACTIONS.has(action))return rawRecord(playerId,action,note,context);return call('record_analyst_action_v08',{p_match_id:runtime.activeMatchId,p_player_id:playerId,p_action:action,p_client_event_id:id(),p_cause:context.cause||null,p_note:String(note||'').trim()||null})})}
 function startPossession(playerId){return enqueue(async()=>{const active=possession();if(active?.playerId===playerId)return {ok:true,idempotent:true,reason:'already_active'};return rawRecord(playerId,'possession_start','Bezit-timer gestart')})}
 function stopPossession(){return enqueue(async()=>{const active=possession();if(!active?.playerId)return {ok:true,idempotent:true,reason:'already_stopped'};return rawRecord(active.playerId,'possession_stop','Bezit-timer gestopt')})}
 function receiveBall(playerId){return enqueue(async()=>{const active=possession(),target=player(playerId);invariant(target?.selected&&target.is_on_field,'Ontvanger moet een veldspeler zijn');if(active?.playerId===playerId)return {ok:true,idempotent:true,reason:'already_has_ball'};if(!active?.playerId)return rawRecord(playerId,'possession_start','Balstroom gestart');const from=player(active.playerId);invariant(from?.is_on_field,'Huidige balbezitter staat niet meer op het veld');return call('record_ball_flow_v08',{p_match_id:runtime.activeMatchId,p_from_player_id:active.playerId,p_to_player_id:playerId,p_client_event_id:id()})})}
 function analystReceiveBall(playerId){return enqueue(async()=>{const active=possession(),target=player(playerId);invariant(target?.selected&&target.is_on_field,'Ontvanger moet een veldspeler zijn');if(active?.playerId===playerId)return {ok:true,idempotent:true,reason:'already_has_ball'};if(!active?.playerId)return call('record_analyst_action_v08',{p_match_id:runtime.activeMatchId,p_player_id:playerId,p_action:'possession_start',p_client_event_id:id(),p_cause:null,p_note:'Analist-balcirculatie gestart'});const from=player(active.playerId);invariant(from?.is_on_field,'Huidige balbezitter staat niet meer op het veld');return call('record_analyst_ball_flow_v08',{p_match_id:runtime.activeMatchId,p_from_player_id:active.playerId,p_to_player_id:playerId,p_client_event_id:id()})})}
 function loseBall(playerId,cause='unknown',note=''){return enqueue(async()=>{invariant(LOSS_CAUSES.includes(cause),'Onbekende oorzaak balverlies');const p=player(playerId);invariant(p?.selected&&p.is_on_field,'Balverlies kan alleen voor een veldspeler');return call('record_ball_loss_v08',{p_match_id:runtime.activeMatchId,p_player_id:playerId,p_cause:cause,p_client_event_id:id(),p_note:String(note||'').trim()||null})})}
 function analystLoseBall(playerId,cause='unknown',note=''){return enqueue(async()=>{invariant(LOSS_CAUSES.includes(cause),'Onbekende oorzaak balverlies');const action=cause==='bad_pass'?'bad_pass':'ball_loss';return call('record_analyst_action_v08',{p_match_id:runtime.activeMatchId,p_player_id:playerId,p_action:action,p_client_event_id:id(),p_cause:cause,p_note:String(note||'').trim()||null})})}
 function startTeamPossession(side){return enqueue(async()=>{invariant(side==='for'||side==='against','Kies ons team of tegenstander');const active=teamPossession();if(active?.side===side)return {ok:true,idempotent:true,reason:'already_active'};return call('record_team_possession_v08',{p_match_id:runtime.activeMatchId,p_action:'start',p_side:side,p_client_event_id:id()})})}
 function stopTeamPossession(){return enqueue(async()=>{const active=teamPossession();if(!active)return {ok:true,idempotent:true,reason:'already_stopped'};return call('record_team_possession_v08',{p_match_id:runtime.activeMatchId,p_action:'stop',p_side:null,p_client_event_id:id()})})}
 function stopAllPossession(){return enqueue(async()=>{invariant(snapshot?.match?.id&&runtime.activeMatchId,'Open eerst een live wedstrijd');if(!possession()&&!teamPossession())return {ok:true,idempotent:true,reason:'already_stopped'};return call('stop_all_possession_v08',{p_match_id:runtime.activeMatchId,p_client_event_id:id()})})}
 return Object.freeze({setSnapshot,record,analystRecord,startPossession,stopPossession,receiveBall,analystReceiveBall,loseBall,analystLoseBall,startTeamPossession,stopTeamPossession,stopAllPossession,currentPossession:possession,currentTeamPossession:teamPossession,get snapshot(){return snapshot},get actions(){return ACTIONS},get lossCauses(){return LOSS_CAUSES}})
}
global.ClubMatchV08PlayerActions={ACTIONS,LOSS_CAUSES,createPlayerActionController,totalSecond,eventOrder};
})(typeof window!=='undefined'?window:globalThis);
